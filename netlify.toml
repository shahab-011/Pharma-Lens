# Root netlify.toml for PharmaLens Frontend Deployment
[build]
  base = "frontendd"
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
